BACKEND_URL=http://127.0.0.1:8000
# Railway: set this to the backend service's private URL, for example
# http://question-paper-api.railway.internal:8000
# Preferred Railway reference variable (when the API service is named api):
# http://${{api.RAILWAY_PRIVATE_DOMAIN}}:${{api.PORT}}
